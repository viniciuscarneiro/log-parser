package com.ef;

import org.junit.runner.RunWith;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

@RunWith(SpringRunner.class)
@SpringBootTest
public class ParserTests {

    /*private String filePath;
    private LocalDateTime startDate;
    private Integer threshold100;
    private Integer threshold200;

    @Before
    public void init() {
        this.filePath = "/Users/brito/Downloads/Java_MySQL_Test/access.log";
        this.startDate = LocalDateTime.now();
        this.threshold100 = 100;
        this.threshold200 = 200;
    }

    @Test
    public void contextLoads() {
    }

    @Test
    public void shouldReadFile() {
        Parser.readFile(filePath, startDate, DurationEnum.DAILY, threshold100);
    }

    @Test(expected = InvalidFileException.class)
    public void shouldNotReadFile() {
        Parser.readFile("", startDate, DurationEnum.DAILY, threshold100);
    }
*/

}
